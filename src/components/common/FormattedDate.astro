---
interface Props {
	date: Date;
  format: 'long' | 'short'
}

const { date, format = 'short' } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: format,
			day: '2-digit',
		})
	}
</time>
